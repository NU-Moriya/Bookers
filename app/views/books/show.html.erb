<% if @book.errors.any? %>
<ul>
  <% @book.errors.full_messages.each do |error| %>
    <li><%= error %></li>
  <% end %>
</ul>
<h2>Title:
<%= @book.title %>
</h2>
<h2>Body:
<%= @book.body %>
</h2>

<%= link_to "Edit", edit_book_path(@book) %>
<%= link_to "Back", books_path(@book) %>